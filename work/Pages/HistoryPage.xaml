<Page x:Class="work.Pages.HistoryPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:work.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="HistoryPage">

    <Grid>
        <ScrollViewer Template="{StaticResource MyScrollViewer}" Grid.Column="0" Grid.Row="1" VerticalAlignment="Top" Grid.ColumnSpan="2" x:Name="scrList" Margin="0" VerticalScrollBarVisibility="Auto" Height="350" Width="400">
            <StackPanel Orientation="Vertical" Name="layerList" ScrollViewer.VerticalScrollBarVisibility="Visible" Width="250" >
                <Button Content="人机对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="人人对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="本地对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="历史记录" Style="{StaticResource ctrlButton}"/>
                <Button Content="人机对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="人人对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="本地对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="历史记录" Style="{StaticResource ctrlButton}"/>
                <Button Content="人机对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="人人对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="本地对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="历史记录" Style="{StaticResource ctrlButton}"/>
                <Button Content="人机对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="人人对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="本地对战" Style="{StaticResource ctrlButton}"/>
                <Button Content="历史记录" Style="{StaticResource ctrlButton}"/>

            </StackPanel>
        </ScrollViewer>
       
    </Grid>
</Page>
